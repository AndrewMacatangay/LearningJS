//Comments can be used to describe how and why some piece of code works

//Bad comments: You shouldn't need to specify what the code does. Instead,
//you should specify how it works

function multiplyBy2One(x)
{
	//Mutiplies a number by two
	return x * 2;
}

//This function uses the arithmeticc left shift to multiply a number by 2
function multiplyBy2Two(x)
{
	return x << 1;
}
